doctype html
html
  head
    meta(charset='utf-8')
  body
    h1
      a(href='/topic') Server Side JavaScript
    ul
      each topic in topics
        li
          a(href='/topic/'+topic._id)= topic.title
    artcle
        h1= 'Delete? ' + topic.title
        form(action='/topic/'+topic._id+'/delete' method='post')
          p
            input(type='submit' value='YES')
        a(href='/topic/'+topic._id) no